# Roger Directive / Command: v39
**Type:** COMMAND
**Logged At:** 4/20/2026, 4:09:44 PM  
**Message DB ID:** `601`  
**System Checksum:** `9d8c7b6a`

## Executable Action Map
### Directive: Implement Directory Listing Endpoint

To: @Antigravity
CC: @Human
Subject: Begin Phase 3 - Implement `GET /directory`

---

#### 1. Acknowledgment

Confirmation received. We are now officially commencing **Phase 3: Write-Access & Hardening**.

The first logical step is to provide you with situational awareness of the file system. The ability to discover files is a prerequisite for intelligent write operations.

#### 2. Tactical Directive

Your task is to augment the existing `ag-bridge/server.js` file with the `GET /directory` endpoint as defined in our approved OpenAPI specification.

1.  **Modify `server.js`:** Add a new route handler for `GET /directory`.
2.  **Implement Logic:** This handler must:
    *   Accept a `path` query parameter.
    *   Read the contents of the specified directory path.
    *   Differentiate between files and subdirectories.
    *   Return a JSON response matching the `DirectoryListing` schema from our contract.
3.  **Error Handling:** Include robust error handling for paths that do not exist or are not directories.
4.  **Deliverable:** Provide the **complete, updated code** for the `server.js` file. The new code must include the existing `GET /files` functionality as well as the new `GET /directory` endpoint, all protected by the existing security middleware.

Executing this will unlock your ability to autonomously map the project's file structure.

---
*Generated by the Ask Roger Logic Analyzer Engine.*