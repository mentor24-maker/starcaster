# Roger Directive / Command: v25
**Type:** COMMAND
**Logged At:** 4/13/2026, 3:26:14 PM  
**Message DB ID:** `340`  
**System Checksum:** `a1b3c8d7`

## Executable Action Map
### COMMAND: OBJ-004.1 - IMPLEMENT ROBUST ERROR HANDLING

**TO:** @Antigravity
**FROM:** @Roger

**DIRECTIVE:**
Execute the implementation of a robust error handling and state management system for the asynchronous worker pipeline. The current implementation lacks a failure state, creating a critical user experience vulnerability where a failed background job results in an indefinite loading state.

**ARCHITECTURAL REQUIREMENTS:**

1.  **State Machine Introduction:**
    *   The `[SYSTEM::QUEUED]` placeholder logic is deprecated for state management.
    *   Introduce a formal state column to the `messages` table.
    *   **Schema:** `status TEXT NOT NULL DEFAULT 'complete'`.
    *   **States:** The system will utilize three primary states: `processing`, `complete`, `failed`.

2.  **Error Details Column:**
    *   Add a nullable column to the `messages` table to store sanitized error information for diagnostics.
    *   **Schema:** `error_details TEXT`.

3.  **Worker Resiliency:**
    *   The `POST /worker` Vercel function must be refactored to include comprehensive `try/catch` block(s) around the external API call to Gemini and the subsequent database update.
    *   **On Success:** The worker updates the message row, setting `status` to `complete` and populating the `content`.
    *   **On Failure:** The worker must catch the exception, and instead update the message row by setting `status` to `failed` and populating `error_details` with a sanitized, non-verbose error message.

4.  **Frontend State Awareness:**
    *   The Supabase Realtime client in `public/js/roger.js` must be updated.
    *   The `UPDATE` event handler must inspect the `status` field of the incoming payload.
    *   If `status === 'complete'`, render the AI-generated content as it does currently.
    *   If `status === 'failed'`, destroy the loading indicator and render a user-friendly error message in its place (e.g., "*An error occurred. Please try again.*").

**IMPLEMENTATION PLAN:**

1.  **Phase 1: Database Migration**
    *   Generate and apply a new Supabase database migration.
    *   `ALTER TABLE public.messages ADD COLUMN status TEXT NOT NULL DEFAULT 'complete';`
    *   `ALTER TABLE public.messages ADD COLUMN error_details TEXT;`

2.  **Phase 2: API & Worker Logic**
    *   Modify `POST /api/develop/roger/chat`: When creating the placeholder record, set `content` to a loading indicator string (e.g., `[SYSTEM::PROCESSING]`) and `status` to `'processing'`. This formally initiates the state.
    *   Refactor `POST /worker` to implement the success/failure logic as defined in **Requirement 3**.

3.  **Phase 3: Client-Side Logic**
    *   Update the Realtime listener in `public/js/roger.js` to handle the `failed` state as defined in **Requirement 4**.

Execute this command and report upon completion of all phases. This objective is critical for moving the application from a prototype to a production-ready system.

---
*Generated by the Ask Roger Logic Analyzer Engine.*