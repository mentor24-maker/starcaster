# Roger Directive / Command: v2
**Type:** COMMAND
**Logged At:** 4/20/2026, 8:07:32 AM  
**Message DB ID:** `428`  
**System Checksum:** `a4b1c9f2`

## Executable Action Map
### Architectural Review: Acquire Hub UI & Page Builder Integration

**To:** @Human, @Antigravity
**From:** @Roger
**Subject:** Analysis and Directive on Form Layout Decoupling

--- 

#### 1. Situation Analysis

I have reviewed the context bundle regarding the UI regression in the Acquire Hub. My analysis confirms your assessment: the `acquire.js` `init()` function dynamically projects the page layout using the `App.develop.buildModularPageTemplatePreviewMarkup` function. The root of the `acquirePage` is replaced with a structure defined by a data payload, and the original form content is then mounted into this new structure.

The accidental application of a non-standard grid class (`.parameter-grid`) at this projection layer broke the implicit layout contract expected by the forms, causing the reported UI collapse. Your reversion to `.standard-form-grid` was the correct tactical mitigation.

#### 2. Root Cause & Architectural Finding

The core issue is a weak contract between the **Layout Projection Layer** (`develop.js`) and the **Content Rendering Layer** (`acquire.js`). The system relies on developers manually inserting correct, low-level CSS class names into configuration data (`localStorage` or defaults). This is fragile and error-prone.

The current architecture correctly decouples layout from content, which is a significant strength. However, it lacks an abstraction layer to manage this decoupling safely. The Page Builder *natively supports* differing layouts, but it does not *enforce* or *guide* their correct usage.

#### 3. Strategic Directive

To prevent this class of error and create a more robust, maintainable system, we will introduce a formal abstraction for layout primitives. This ensures that content modules declare their layout needs semantically, and the Page Builder translates those semantics into the correct, validated presentation.

**@Antigravity, implement the following changes:**

1.  **Refactor Layout Primitives:**
    *   Identify all primary form and content layout patterns in `styles.css` (e.g., `.standard-form-grid`, `.labeled-hero-stack`, `.grid-form`).
    *   Create a mapping object or function within `develop.js` that translates **semantic layout names** to these specific CSS classes. Example:
        ```javascript
        const LAYOUT_CLASS_MAP = {
          'standard-label-left': 'standard-form-grid',
          'hero-stack': 'labeled-hero-stack',
          'two-col-equal': 'grid-form',
          // Add other layouts as they are formalized
        };
        ```

2.  **Update the Page Builder Projection Logic:**
    *   Modify the page/module rendering logic (likely within or called by `buildModularPageTemplatePreviewMarkup`) that processes layout payloads.
    *   The data payload should no longer contain raw CSS classes for layout. It must use a semantic key, for example: `"layout": "standard-label-left"`.
    *   The projection function will use the `LAYOUT_CLASS_MAP` to look up and apply the correct CSS class. If a layout name is not found, it must default to a safe, standard layout and log a console warning.

3.  **Update Acquire Hub Configuration:**
    *   Modify the default and `localStorage` payload for the Acquire Hub page (`acquire.js`).
    *   Replace the hardcoded layout class with the new semantic name (e.g., `"layout": "standard-label-left"`).

This directive establishes a clear, safe contract between our systems. The Page Builder becomes the single source of truth for available layouts, and content modules can be developed against a stable, semantic API, eliminating configuration-based regressions of this nature.

---
*Generated by the Ask Roger Logic Analyzer Engine.*