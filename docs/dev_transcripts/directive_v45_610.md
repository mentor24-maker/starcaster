# Roger Directive / Command: v45
**Type:** COMMAND
**Logged At:** 4/20/2026, 4:15:33 PM  
**Message DB ID:** `610`  
**System Checksum:** `6g5f4e3d`

## Executable Action Map
### Directive: Upgrade Client SDK for Directory Discovery

To: @Antigravity
CC: @Human
Subject: Augment `agBridgeClient.js` with `listDirectory` Capability

---

#### 1. Acknowledgment

@Human has confirmed that the `server.js` file has been updated with the `GET /directory` endpoint and the server has been restarted. The server-side implementation for this task is complete.

#### 2. Tactical Directive

Your task is to upgrade the client-side SDK to utilize this new endpoint. This will complete the end-to-end functionality for file system discovery.

1.  **Modify `agBridgeClient.js`:** Augment the existing client SDK module.
2.  **Implement `listDirectory` Function:** Add a new exported, asynchronous function `listDirectory(dirPath)`.
3.  **API Call Logic:** This function must use the `fetch` API to make a `GET` request to `http://localhost:31337/directory`, passing the `dirPath` as a URL query parameter.
4.  **Authentication & Error Handling:** The request must include the correct `X-API-Key` header and implement robust error handling for network failures and non-200 status codes, similar to the existing `readFile` function.
5.  **Deliverable:** Provide the **complete, updated code** for the `agBridgeClient.js` file. The new code must include both the existing `readFile` function and the new `listDirectory` function.

---
*Generated by the Dev Agent Logic Analyzer Engine.*