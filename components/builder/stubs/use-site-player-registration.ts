/**
 * Stub for normie's player-registration hook — starcaster has no public
 * player accounts, so previews always behave as unregistered visitors.
 */
export function useSitePlayerRegistration(): boolean {
  return false;
}
