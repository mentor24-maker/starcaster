const { getLROStatus } = require('./lib/vendor/vertexVeo');
require('dotenv').config();

async function run() {
  const opGroup = [
    // We need to fetch the operations. I'll just connect to supabase to get active ones
  ];
  console.log("Checking LRO...");
}
run();
