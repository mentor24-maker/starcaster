#!/bin/sh
# Refuse to push a branch that carries another branch's unshipped commits
# (branched off the wrong place). Bypass with SKIP_PASSENGER_CHECK=1.
set -e
ROOT="$(git rev-parse --show-toplevel)"
cd "$ROOT"
node scripts/check_branch_passengers.cjs
