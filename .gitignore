node_modules
.DS_Store
npm-debug.log*
.vercel

# Environment files — never commit secrets
.env
.env.*
!.env.example

# Local JSON stores that hold credentials or user data
data/api_settings.json
data/auth_users.json
data/auth_sessions.json
# Local fallback store for the Event Calendar (Supabase is the real home).
data/events.json

# Local-only files
docs/archives/
files/google_console_oauth_client_secret_*.json
starcaster-cookies.txt
*.pxz

# Build artifacts — generated by `npm run build`; never commit.
# Vercel and CI rebuild these from source on every deploy.
public/app-shell.html
public/about.html
public/site.html
public/explore.html
public/builder-preview.html
public/privacy-policy.html
public/terms-of-service.html
public/data-deletion.html
public/styles.css
public/bundle.js
public/builder-bundle.js
public/js/richtext-vendor.js
lib/builder/template.js
lib/builder/email-template.js
lib/builder/template-frame.js
lib/builder/email-render.js
lib/build-stamp.json
lib/site-import/dist/

# Claude Code: settings.json and skills are shared; everything else is personal
.claude/*
!.claude/settings.json
!.claude/skills/
docs/SQL/backups/

# Supabase CLI local state — machine-specific, not project source
supabase/.branches/
supabase/.temp/

# Screenshots from `npm run check:screens` — local artifacts, never committed
.ui-check/

# Playwright scratch profile written by scripts/capture_platform_screenshots.js
.playwright/

# Records when this laptop last ran `npm run db:refresh` (see scripts/doctor.mjs)
.local-db-refreshed

# Before/after screenshots from `npm run check:shots` — local artifacts, never committed
.ui-shots/
